---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  namespace: stage
  name: sp-demo-virtual-service
spec:
  hosts:
    - "*"
  gateways:
    - sp-demo-gateway
  http:
    - route:
        - destination:
            host: spr-demo-api.stage.svc.cluster.local
            subset: v1
          weight: 50
        - destination:
            host: sp-demo-api.stage.svc.cluster.local
            subset: canary
          weight: 50